# 分散式路徑最佳化平台架構介紹指南

這份指南以淺顯易懂的方式，為您梳理本專案的「核心組件」與「實現方式」，方便您向他人介紹。

---

## 1. 核心組件介紹

### 🖥️ 前端網頁 (Frontend UI)
*   **檔案位置**：`master/public/` (`index.html`, `style.css`, `app.js`)
*   **負責工作**：
    *   **地圖互動**：讓使用者在畫布上自由點選多個「配送點」（點位）。
    *   **即時監控**：每秒發送 API 請求，更新 Master 與各個 Worker 節點目前的**運算狀態**與**硬體負載（CPU / 記憶體）**。
    *   **動畫模擬**：在任務完成後，以動畫形式在地圖上模擬最優配送路徑。

---

### 👑 Master 主節點 (協調與調度者)
*   **檔案位置**：[master/server.py](./master/server.py)
*   **負責工作**：
    *   **任務拆分（分治法）**：將一個大型的旅行推銷員（TSP）路徑問題，拆解成多個「指定第一步走法不同」的子任務。
        *   *舉例：若要走訪 5 個城市，Master 會生成「先走城市 A」、「先走城市 B」等子任務分開求解。*
    *   **任務分派與負載平衡**：透過背景執行緒持續檢查，一旦有 Worker 閒置（`online`），就將排隊中的子任務發送給它。
    *   **結果整合**：收集各 Worker 回傳的子任務結果，比對並挑選出「總距離最短」的最優路徑。
    *   **心跳監控**：定期向各 Worker 發送請求，記錄它們的健康狀態與回傳的硬體遙測指標。

---

### ⚙️ Worker 運算節點 (核心計算者)
*   **檔案位置**：[worker/worker.py](./worker/worker.py) 及 [worker/solver.py](./worker/solver.py)
*   **負責工作**：
    *   **基因演算法（GA）求解**：[solver.py](./worker/solver.py) 使用「適者生存」的演化概念，透過突變與雜交在極短時間內找出近似最優的配送路徑。
    *   **避開 GIL 的多進程設計 (`multiprocessing`)**：使用 Python 的 `multiprocessing` 模組，在收到任務時會**獨立啟動一個進程 (Process)** 去執行求解器。這樣做能有效避開 Python 的 **GIL (全域解釋器鎖，規定同一個 Python 程序內一次只能有一個執行緒進行 CPU 計算的限制)**，確保繁重的計算指派到其他 CPU 核心，而主網頁伺服器 (Flask) 仍能流暢地即時回應 Master 的心跳偵測與取消指令。
    *   **硬體資源遙測**：每秒讀取容器內 Linux Cgroups 目錄的實體數據，精確回報自身容器的 CPU 核心使用率及記憶體實際用量百分比。

---

## 2. 專案技術亮點（簡報精華）

1.  **分散式分治計算（Parallel Divide & Conquer）**
    *   大型 TSP 問題運算量極大。我們藉由限制子任務的「第一步」，將計算空間切碎，同時分派給多個 Worker 進行平行計算，大幅縮短整體求解時間。
2.  **Docker 容器化與高擴展性**
    *   使用 `docker-compose` 快速部署。若計算量增加，只需在 Compose 檔案中多複製幾個 Worker 容器，Master 就會自動將新節點納入調度體系中，具備極佳的水平擴展性（Scale-out）。
3.  **精確的容器資源監控 (Cgroups Telemetry)**
    *   透過直接讀取容器底層 Linux Cgroups v1/v2 的統計檔案，排除宿主機其他軟體的干擾，在網頁前端看板上呈現每個 Container 最真實、精確的硬體效能數據。
